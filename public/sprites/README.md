Blender output goes here.

Generate with:

    blender --background --python tools/blender/reactor.py

The app falls back to the vector cutaway if these files are missing, so it is safe to commit
this folder empty.
